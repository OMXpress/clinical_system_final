enum CrudType
{
    insert,
    update,
    delete
};

enum ClinicType
{
    physio,
    ortho
};

enum Gender
{
    male,
    female,
}
enum MaritialStatus
{
    Married,
    Divorced,
    Widowed,
    Single
}
enum JsonType
{
    History,
    MetaData,
    Prescriptions,
    Visits
}